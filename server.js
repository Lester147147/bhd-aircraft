const http=require('http');const fs=require('fs');const path=require('path');const PORT=process.env.PORT||3000;const BHD={lat:54.6181,lon:-5.8725,radius:50};
function send(res,status,type,body){res.writeHead(status,{'Content-Type':type,'Cache-Control':'no-store'});res.end(body)}
const sources=[
{name:'adsb.lol',url:`https://api.adsb.lol/v2/point/${BHD.lat}/${BHD.lon}/${BHD.radius}`},
{name:'airplanes.live',url:`https://api.airplanes.live/v2/point/${BHD.lat}/${BHD.lon}/${BHD.radius}`},
{name:'adsb.fi',url:`https://opendata.adsb.fi/api/v2/lat/${BHD.lat}/lon/${BHD.lon}/dist/${BHD.radius}`}
];
async function getAircraft(){const errors=[];for(const s of sources){try{const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),8000);const r=await fetch(s.url,{headers:{'User-Agent':'BHD-Aircraft-Radar/1.1','Accept':'application/json'},signal:controller.signal});clearTimeout(timer);const text=await r.text();if(!r.ok){errors.push(`${s.name}:${r.status}`);continue}const d=JSON.parse(text);const ac=Array.isArray(d.ac)?d.ac:(Array.isArray(d.aircraft)?d.aircraft:null);if(ac)return {source:s.name,ac,total:ac.length,now:d.now||Date.now()/1000};errors.push(`${s.name}:bad-shape`)}catch(e){errors.push(`${s.name}:${e.name==='AbortError'?'timeout':e.message}`)}}throw new Error(errors.join(' | '))}
const server=http.createServer(async(req,res)=>{try{const u=new URL(req.url,'http://localhost');if(u.pathname==='/api/aircraft'){try{const data=await getAircraft();return send(res,200,'application/json',JSON.stringify(data))}catch(e){return send(res,502,'application/json',JSON.stringify({error:'All aircraft feeds unavailable',detail:e.message}))}}if(u.pathname==='/'||u.pathname==='/index.html'){return send(res,200,'text/html; charset=utf-8',fs.readFileSync(path.join(__dirname,'index.html'),'utf8'))}if(u.pathname==='/manifest.webmanifest'){return send(res,200,'application/manifest+json',fs.readFileSync(path.join(__dirname,'manifest.webmanifest'),'utf8'))}send(res,404,'text/plain','Not found')}catch(e){send(res,500,'application/json',JSON.stringify({error:e.message}))}});server.listen(PORT,()=>console.log(`BHD radar listening on ${PORT}`));